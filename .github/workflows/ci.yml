name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.cursor/**'
      - 'docs/**'
      - 'CHANGELOG*'
      - 'LICENSE'
      - '.vscode/**'
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.cursor/**'
      - 'docs/**'
      - 'CHANGELOG*'
      - 'LICENSE'
      - '.vscode/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate (Lint, Typecheck, Unit Test)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type Check
        run: npm run type-check

      - name: Unit Tests
        run: npm test

  e2e-test:
    name: E2E Tests (iOS)
    runs-on: macos-latest
    timeout-minutes: 30
    if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'e2e-test')
    env:
      EXPO_PUBLIC_SUPABASE_URL: "https://example.supabase.co"
      EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY: "mock-key"
      EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY: "mock-key"
      # Increase to 10 minutes to handle slower CI environments
      MAESTRO_DRIVER_STARTUP_TIMEOUT: 600000

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # Generate cache keys for better granularity
      - name: Generate Cache Keys
        id: cache-keys
        run: |
          # Use package-lock.json as primary cache key, Podfile.lock may not exist yet
          PODS_HASH="${{ hashFiles('package-lock.json') }}"
          BUILD_HASH="${{ hashFiles('app/**', 'components/**', 'lib/**', 'stores/**', 'package-lock.json') }}"
          echo "pods-key=${{ runner.os }}-pods-${PODS_HASH}" >> $GITHUB_OUTPUT
          echo "build-key=${{ runner.os }}-ios-build-${BUILD_HASH}" >> $GITHUB_OUTPUT

      # Cache the entire Pods folder for faster pod install
      - name: Cache Pods
        id: cache-pods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ steps.cache-keys.outputs.pods-key }}
          restore-keys: |
            ${{ runner.os }}-pods-

      # Cache CocoaPods global cache as fallback
      - name: Cache CocoaPods Specs
        uses: actions/cache@v4
        with:
          path: ~/.cocoapods
          key: ${{ runner.os }}-cocoapods-specs-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-cocoapods-specs-

      # Cache the built app to skip rebuild if source hasn't changed
      - name: Cache iOS Build
        id: cache-ios-build
        uses: actions/cache@v4
        with:
          path: |
            ios/build/Build/Products/Release-iphonesimulator
            ios/build/ModuleCache.noindex
          key: ${{ steps.cache-keys.outputs.build-key }}
          restore-keys: |
            ${{ runner.os }}-ios-build-

      - name: Install Node Dependencies
        run: npm ci

      # Prebuild generates the native project (ios/ directory)
      # Always prebuild to ensure ios/ directory is properly generated
      - name: Prebuild iOS
        run: |
          if [ ! -f "ios/Podfile" ]; then
            echo "Podfile not found, running prebuild..."
            npx expo prebuild --platform ios --clean --no-install
          else
            echo "iOS project already exists (Podfile found), skipping prebuild"
          fi

      # Only run pod install if cache missed or Podfile.lock changed
      - name: Install Pods
        if: steps.cache-pods.outputs.cache-hit != 'true'
        run: |
          if [ ! -d "ios" ]; then
            echo "Error: ios directory not found. Prebuild may have failed."
            exit 1
          fi
          cd ios
          pod install --verbose

      # Boot simulator early in parallel with other setup
      - name: Boot Simulator
        run: |
          # Boot simulator and wait for it to be fully ready
          xcrun simctl boot "iPhone 16" || echo "Simulator already booted"
          
          echo "Waiting for simulator to fully boot..."
          xcrun simctl bootstatus "iPhone 16"
          
          # Additional settling time for simulator UI to be ready
          echo "Allowing simulator UI to settle..."
          sleep 15
          
          echo "Simulator is ready"

      - name: Install Maestro
        run: |
          curl -fsSL "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> $GITHUB_PATH

      # Display cache status
      - name: Check Build Cache Status
        run: |
          if [ "${{ steps.cache-ios-build.outputs.cache-hit }}" == "true" ]; then
            echo "✓ Build cache hit! Skipping build step (saves ~8 minutes)"
          else
            echo "✗ Build cache miss. Will build from scratch."
          fi

      # Build only if cache missed
      - name: Build iOS App
        if: steps.cache-ios-build.outputs.cache-hit != 'true'
        run: |
          cd ios
          WORKSPACE=$(find . -maxdepth 1 -name "*.xcworkspace" | head -n 1)

          if [ -z "$WORKSPACE" ]; then
            echo "Error: No .xcworkspace found"
            exit 1
          fi

          SCHEME="tuberushv2"
          echo "Building Workspace: $WORKSPACE with Scheme: $SCHEME"

          # Optimized build settings for CI
          set -o pipefail
          xcodebuild -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            -derivedDataPath build \
            -jobs $(sysctl -n hw.ncpu) \
            ONLY_ACTIVE_ARCH=YES \
            DEBUG_INFORMATION_FORMAT=dwarf \
            COMPILER_INDEX_STORE_ENABLE=NO \
            ENABLE_BITCODE=NO \
            GCC_OPTIMIZATION_LEVEL=s \
            SWIFT_OPTIMIZATION_LEVEL=-O \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | tee build.log

      - name: Upload Build Log on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: ios/build.log

      - name: Install App on Simulator
        run: |
          APP_PATH=$(find ios/build/Build/Products/Release-iphonesimulator -name "*.app" | head -n 1)

          if [ -z "$APP_PATH" ]; then
            echo "Error: No .app found in build directory"
            exit 1
          fi

          echo "Installing App from: $APP_PATH"
          xcrun simctl install "iPhone 16" "$APP_PATH"

      - name: Pre-launch App
        run: |
          echo "Pre-launching app to ensure it's ready..."
          xcrun simctl launch "iPhone 16" com.tuberush.v2
          sleep 3
          echo "App launched successfully"

      - name: Run Maestro Flows
        run: |
          maestro test .maestro/flow.yaml .maestro/auth_flow.yaml --format junit --output report.xml

      - name: Upload Maestro Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-artifacts
          path: |
            report.xml
            ~/.maestro/tests/**/*
